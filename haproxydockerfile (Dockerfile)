FROM haproxy:latest

# Copy your custom HAProxy configuration into the container
COPY haproxy.cfg /usr/local/etc/haproxy/haproxy.cfg

# Expose HAProxy ports (HTTP and HTTPS)
EXPOSE 80
EXPOSE 443

# Start HAProxy
CMD ["haproxy", "-f", "/usr/local/etc/haproxy/haproxy.cfg"]
